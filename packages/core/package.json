{
  "name": "@my-monorepo/core",
  "version": "0.0.1",
  "description": "",
  "main": "dist/index.ts",
  "types": "dist/index.ts",
  "exports": {
    ".": {
      "types": "./dist/index.ts",
      "import": "./dist/index.ts"
    },
    "./preset": {
      "types": "./dist/preset.d.ts",
      "import": "./dist/preset.js",
      "require": "./dist/preset.cjs"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup src/index.ts src/preset.ts --format=esm,cjs --clean --dts --external @pandacss/dev",
    "build:index": "mkdir dist && cp src/index.ts dist",
    "build:preset": "tsup src/preset.ts --format=esm,cjs --dts --external @pandacss/dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@my-monorepo/button": "^0.0.1",
    "@my-monorepo/preset-base": "^0.0.1",
    "@my-monorepo/some-component": "^0.0.1"
  },
  "devDependencies": {
    "@my-monorepo/tsconfig": "^0.0.1",
    "@pandacss/dev": "^0.15.1",
    "eslint": "^8.50.0",
    "eslint-config-custom": "^0.0.1"
  }
}