---
import { css } from "@my-monorepo/ui-lib/css";

interface Props {
  title?: string;
}
const { title } = Astro.props;
---

<div
  class={css({
    layerStyle: "box",
    p: 8,
  })}
>
  {title && <h2 class={css({ textStyle: "headings" })}>{title}</h2>}
  <slot />
</div>
